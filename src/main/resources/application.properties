spring.application.name=equipement

# Ensure Spring Batch creates its metadata tables automatically (Postgres)
spring.batch.jdbc.initialize-schema=always

# Use a different port to avoid conflicts seen earlier
server.port=9091
